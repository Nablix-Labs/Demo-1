services:
  - type: web
    name: demo-1-backend
    runtime: python
    rootDir: nablix-backend
    buildCommand: pip install -r requirements.txt
    startCommand: python -m uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: NABLIX_USE_MOCK_VISION
        value: "true"
